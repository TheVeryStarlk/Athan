<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prayers="clr-namespace:Athan.Avalonia.Features.Prayers"

             x:Class="Athan.Avalonia.Features.Prayers.PrayerView"
             x:DataType="prayers:PrayerViewModel">

    <StackPanel>
        <TextBlock Text="{CompiledBinding Next.Name, FallbackValue='Death'}" />

        <ListBox ItemsSource="{CompiledBinding Prayers}">
            <ListBox.ItemTemplate>
                <DataTemplate x:DataType="prayers:Prayer">
                    <Grid ColumnDefinitions="* *">
                        <TextBlock Text="{CompiledBinding Name}" FontWeight="Bold" Grid.Column="0" />

                        <StackPanel Orientation="Horizontal" Spacing="5" HorizontalAlignment="Right" Grid.Column="1">
                            <TextBlock Text="{CompiledBinding Message}" Opacity="0.75" />
                            <TextBlock Text="{CompiledBinding Time}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</UserControl>